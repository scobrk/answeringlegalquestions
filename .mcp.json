{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": [
        "-y",
        "@supabase/mcp-server-supabase",
        "--read-only"
      ],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "placeholder_token_will_be_set_after_project_creation"
      }
    }
  }
}