# Lightweight requirements for Vercel deployment with Supabase
# Memory-optimized for 512MB per function (total ~100MB usage)

# Core dependencies only - NO ML libraries (moved to Supabase)
openai>=1.3.0
python-dotenv>=1.0.0
requests>=2.31.0

# Supabase client for vector operations
supabase>=2.0.0

# Basic utilities
markdown>=3.5.1